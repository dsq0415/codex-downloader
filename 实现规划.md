# Codex 下载站 · 实现规划

## 接口调研结果

### 下载链接（全部用 agentsmirror CDN，国内自动走中科院高能所镜像，不需要额外处理）

**完整软件安装包（Codex 本体）：**
| 平台 | 链接 | 格式 |
|------|------|------|
| Windows x64 | `https://codexapp.agentsmirror.com/latest/win-x64` | .Msix |
| macOS Apple Silicon | `https://codexapp.agentsmirror.com/latest/mac-arm64` | .dmg |
| macOS Intel | `https://codexapp.agentsmirror.com/latest/mac-intel` | .dmg |

**下载管理器（Codex App Manager）：**
| 平台 | 链接 | 格式 |
|------|------|------|
| Windows x64 | `https://codexapp.agentsmirror.com/manager/latest/CodexAppManager_x64-setup.exe` | .exe |
| macOS Apple Silicon | `https://codexapp.agentsmirror.com/manager/latest/CodexAppManager_aarch64.dmg` | .dmg |
| macOS Intel | `https://codexapp.agentsmirror.com/manager/latest/CodexAppManager_x86_64.dmg` | .dmg |

**关键点：**
- 所有链接都是"最新版"短链，永远指向最新 release，不需要我们维护版本号
- 国内用户访问时 Cloudflare 自动 302 到中科院高能所 S3 镜像（`fgws3-ocloud.ihep.ac.cn`），速度快
- 我们只需要在前端写死这些链接，用户点击即触发浏览器下载

---

## 技术方案

### 文件结构

```
E:\projects\money\codex\
├── index.html      ← 唯一文件，所有代码都在这里
└── 需求文档.md
```

**单文件方案**。HTML + CSS + JS 全写在一个 index.html 里。
原因：Gitee Pages 纯静态，单文件最简单，部署就是把这个文件扔上去。

### 功能模块（都在 index.html 内）

#### 1. 验证码模块
```js
// 改密码只改这一行
const CODES = ["123456"];
```
- 验证逻辑：输入值 trim + 转小写后跟数组比对
- 成功后写 localStorage（7天过期）
- 下次打开自动跳过验证

#### 2. 系统检测
```js
const isMac = navigator.platform.includes("Mac");
```
- Mac → 大按钮显示"Mac 版下载"，小字显示"Windows 版点这里"
- 非 Mac → 反过来
- Mac 还需要区分 Intel/ARM？→ 不区分，直接用 mac-arm64（Apple Silicon 是主流，Intel Mac 也能通过 Rosetta 跑）
  - 实际上 agentsmirror 的 `/latest/mac-arm64` 和 `/latest/mac-intel` 是分开的
  - 方案：Mac 大按钮用 arm64，小字链接旁边加个"Intel Mac 点这个"

#### 3. 下载触发
- 点按钮 → `window.open(url, "_blank")` 或直接 `<a href download>`
- 同时弹出弹窗
- 不跳转当前页面

#### 4. 弹窗（下载后推广）
- 纯 CSS + JS 实现，无框架
- 遮罩 + 居中白色卡片
- 内容：正在下载提示 + 良心云推广 + 教程链接
- ✕ 和"确定"都关闭弹窗
- 淡入动画

#### 5. 首屏三卡片
- 未验证：下载和教程灰色+锁，魔法上网彩色可点
- 已验证：下载和教程解锁（显示下载区），魔法上网不变

### 外部依赖

**零依赖。** 不用任何框架、不用 CDN 加载 jQuery/React。
纯 HTML + CSS + 原生 JS。加载速度最快，不会因外部资源挂掉而白屏。

字体用系统默认，图标用 emoji。

---

## 页面结构（HTML 骨架）

```
<body>
  <!-- 首屏：标题 + 三卡片 + 验证码输入 -->
  <section id="hero">
    <h1>Codex 一键下载站</h1>
    <p>不需要翻墙 · 不需要注册 · 下载就能用</p>
    <div class="cards">
      <div class="card locked">📥 下载 Codex · 🔒 输入验证码解锁</div>
      <div class="card locked">📖 教程 · 🔒 输入验证码解锁</div>
      <div class="card active">🌐 魔法上网 · ✅ 点击查看 →</div>
    </div>
    <div class="code-input">
      <input placeholder="输入验证码，解锁下载和教程">
      <button>解锁</button>
    </div>
  </section>

  <!-- 下载区（验证后显示） -->
  <section id="download" hidden>
    <p>✅ 检测到你的电脑是 Windows</p>
    <div class="download-cards">
      <div>📦 完整软件安装包【推荐】... [Windows版下载] Mac版点这里</div>
      <div>🧰 下载管理器 ... [Windows版下载] Mac版点这里</div>
    </div>
  </section>

  <!-- 弹窗 -->
  <div id="modal" hidden>
    <div class="modal-content">
      ✕
      ✅ 已开始下载
      ⚡ 魔法上网推广（了解一下 →）
      📖 教程链接
      [确定]
    </div>
  </div>
</body>
```

---

## 部署步骤

1. 在 Gitee 创建仓库（如 `codex-download`）
2. 把 index.html 推到仓库
3. 仓库设置 → 服务 → Gitee Pages → 启动
4. 得到地址：`https://你的用户名.gitee.io/codex-download/`
5. 这个地址就是闲鱼上发的网址

**注意：** Gitee Pages 现在可能需要实名认证才能开通，部署前确认一下。

---

## 开发顺序

1. 写 index.html 完整页面（结构 + 样式 + 交互）
2. 本地浏览器打开测试（双击就能看效果）
3. 你确认效果
4. 推到 Gitee，开 Pages

---

## 风险点

| 风险 | 应对 |
|------|------|
| agentsmirror 链接挂了 | 链接是他们的 CDN，挂了是他们的问题，我们换链接就行（改一行） |
| Gitee Pages 被墙/限速 | Gitee 是国内服务，不会。但 Pages 偶尔抽风，备选：Vercel/Netlify（需翻墙访问） |
| MSIX 格式用户不会装 | MSIX 双击就能装（Win10+），弹窗里可以加一句"下载完双击打开就行" |
| 密码被传播 | 无所谓，目标就是小白用户，传了就换密码重新部署 |
